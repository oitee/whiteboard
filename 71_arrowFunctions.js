var a = 1;
var b = 10;
/*let anotherFunc = () => true;// eg of a function without any parameter
let x = a => a += 1; //eg of a function with single parameter and a single expression
let myFunc = (a, b) => a + b;// eg of a function with multiple parameters but a single line of expression
let newFunc = (a, b) => {// eg of a function with multiple parameters and multiple expressions
    let c = a - b;
    if (c < 0){
        return b + " is greater than " + a;
    }
    return b + "either equal to or less than " + a;
}*/
const sum = function (n, m){
    return n + m;
}
console.log(sum(1,2));//output:3



        