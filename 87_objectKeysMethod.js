//testing out the method Object.keys()
let testObj = { name: "testObj", properties: 3, purpose: "Object.keys" };
let keys = Object.keys(testObj);
console.log(keys);
/*Output:
[ 'name', 'properties', 'purpose' ]
*/
